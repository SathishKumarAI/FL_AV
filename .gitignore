# Images
*.jpg
*.jpeg
*.png
*.bmp
*.tif
*.tiff
*.heic
*.JPG
*.JPEG
*.PNG
*.BMP
*.TIF
*.TIFF
*.HEIC

# Ignore all large files above 100MB to prevent GitHub push issues
*.[0-9][0-9][0-9]MB
*.[0-9][0-9][0-9][0-9]MB
# logs/yolo_conversion.log
# json_to_yolo/bdd100k_labels_json/yolo_conversion_json_to_yolov5_100k.log

# yolov5_test/
Fedml/
flower/
run/
# data/data_bddk100
data/bdd100k/
data/bdd10k/
my-project/data
# test/

# model/
# Videos
*.mp4
*.mov
*.MOV
*.avi

# JSON files (except for setup.cfg and cfg/yolov3*.cfg)
*.json
!/setup.cfg
!/cfg/yolov3*.cfg

# Large files (use Git LFS for files larger than 50MB)
*.{mp4,mov,avi,data,json,mat,weights,pt,pb,onnx,engine,mlmodel,torchscript,tflite,h5} diff=lfs merge=lfs -text

# Ignore all files larger than 50MB (use Git LFS for large files)
* diff=lfs merge=lfs -text

# Allow .ipynb files (Jupyter Notebook) of any size
!*.ipynb


coco128.zip
mpich-4.2.0rc2.tar.gz
mpich-4.2.0rc2.tar.gz:Zone.Identifier

# CSV files with names starting with "results"
results*.csv

# Datasets
coco/
coco128/
VOC/

# MATLAB files (except for targets*.mat)
*.m~
*.mat
!/targets*.mat

# GitHub Python GitIgnore
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
/wandb/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Translations
*.mo
*.pot

# Django and Flask
# *.log
local_settings.py
instance/
.webassets-cache

# Scrapy
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version
.venv*
venv*/
ENV*/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/

# macOS-specific
.DS_Store
.AppleDouble
.LSOverride
Icon
Icon?
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk




# Runtime-generated data configs (materialize_data_yaml)
**/data.runtime.yaml

# Federated global-model checkpoints
checkpoints/

# TLS certificates (secrets)
certs/

# --- pipeline component -------------------------------------------------
# Generated data and run artifacts. None of this belongs in the repo: the fleet
# shards are hardlinks to a 7.6 GB dataset, the MLflow store holds model
# artifacts, and reports are per-run output.
pipeline/vehicles/
pipeline/mlruns/
pipeline/reports/
pipeline/.state/
pipeline/**/__pycache__/

# Credentials, belt and braces. The pipeline reads none of these -- kagglehub
# downloads anonymously -- but a stray copy must never become committable.
kaggle.json
**/kaggle.json
.env
*.pem
*.key
!my-project/certs/.gitkeep

# my-project's loggers use CWD-relative paths, so running pytest from the repo
# root (rather than from my-project/) drops stray per-process log files here.
# Narrow on purpose: logs/installation.log is tracked and must stay.
/logs/*.*.log
/logs/server.log
/logs/client.log
/logs/task.log
/logs/get_set.log
/logs/metrics.log
