# archive/

Retired code kept for reference instead of being deleted (project policy:
**verify, then archive — don't delete**). Nothing here is imported by the live
application.

| File | Why retired |
|------|-------------|
| `server_app_copy.py.bak` | Stale duplicate of an early `server_app.py` with the pre-fix (params-only) weight functions. Risked accidental import; superseded by `my_project/server_app.py`. |
